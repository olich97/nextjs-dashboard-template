import type { NavItem } from "@/types";
import { templateConfig } from "@/template.config";

// Navigation items from template configuration
export const navItems: NavItem[] = templateConfig.navigation;
